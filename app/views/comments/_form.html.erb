<%= form_with(model: [ post, post.comments.build ]) do |form| %>
   <div class="form-control">
     <%= form.text_area :body, placeholder: "Add a comment...", rows: 3, cols: 80%>
     <br>
     <%= form.submit "Post", class: "btn", style: "background: #003D7E" %>
   </div>
<% end %>
